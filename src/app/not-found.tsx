'use client';

function NotFoundPage() {
  console.log('Not found page');
  // needs style wrapper
  // return (
  //     <PageNotFound icon={SVGAsset.Warning} title="Not found" message="Page not found, please try again later" />
  // )

  return <div>Not found</div>;
}

export default NotFoundPage;
